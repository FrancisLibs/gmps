{% extends "base.html.twig" %}

{% block body %}
    <div class="row">
        <div class="col-6">
            <h3 class="mt-4 mx-auto">Commandes en retard</h3>
            <div>
                <table class="table table-striped table-sm">
                    <thead class="js-filter-sorting">
                        {% include 'default/_sorting.html.twig' %}
                    </thead>
                    <tbody class="js-filter-content">
                        {% include 'default/_orders.html.twig' %}
                    </tbody>
                </table>
            </div>
        </div>
    </div> 
{% endblock %}