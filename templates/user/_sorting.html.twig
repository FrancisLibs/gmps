<tr>
    <th scope="col">{{ knp_pagination_sortable(users, 'Identifiant', 'u.username') }}</th>
    <th scope="col" class="text-center">{{ knp_pagination_sortable(users, 'Rôles', 'u.roles') }}</th>
    <th scope="col" class="text-center">{{ knp_pagination_sortable(users, 'Email', 'u.email') }}</th>
    <th scope="col" class="text-center">{{ knp_pagination_sortable(users, 'Téléphone', 'u.phoneNumber') }}</th>
    <th scope="col" class="text-center">{{ knp_pagination_sortable(users, 'Prénom', 'u.firstName') }}</th>
    <th scope="col" class="text-center">{{ knp_pagination_sortable(users, 'Nom', 'u.lastName') }}</th>
</tr>